# NIGHTFALL desktop shell

This Electron wrapper serves the production `dist/` folder from a local-only
HTTP server and displays it in a standalone application window. It intentionally
includes every Vite entrypoint, including `hidden-final-boss.html`.
